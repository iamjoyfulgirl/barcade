const User = require('./User');
const { Message } = require('./Message');
const { Score } = require('./Score');
const Chat = require('./Chat');
const Game = require('./Game');

module.exports = { User, Message, Score, Chat, Game };